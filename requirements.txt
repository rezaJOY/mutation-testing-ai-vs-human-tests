# Core pipeline dependencies — install these first
mutmut==2.4.4
pytest>=7.0,<9.0
openai>=1.0.0

# Target projects must be installed in editable mode AFTER cloning.
# Do NOT add schedule, boltons, or jmespath here.
# Run these after Step 5 (clone target projects):
#
#   pip install -e projects/schedule
#   pip install -e projects/boltons
#   pip install -e projects/jmespath.py
#
# Editable installs make Python import from the cloned source directly,
# so mutmut's in-place mutations are actually executed by the test suite.
# Installing from pip (site-packages) breaks mutation detection entirely.
